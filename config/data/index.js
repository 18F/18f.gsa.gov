// Add data sources here
const dataPlugin = (eleventyConfig) => { /* eslint-disable-line no-unused-vars */
  // eleventyConfig.addGlobalData('key_to_the_data', async () => {
  //   return Promise.resolve(get the data here)
  // });
}

module.exports = dataPlugin;
